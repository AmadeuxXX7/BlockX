BlockX is a block language inpired by Scratch.

This proyect is made in Python and is in the version 0.2.2.

In the code you can create the blocks using the module editor.py, the module bloques.py reads it and makes the block, this module also includes the save system that is called in the menu in main.py.
